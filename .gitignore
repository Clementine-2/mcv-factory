# ============================================================
# Project Factory — .gitignore
# Open-source source tree only. Build outputs, virtual
# environments, installers and agent working dirs are excluded.
# ============================================================

# ---- .NET / WPF build outputs ----
bin/
obj/
publish_win64/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.eggs/
build/
dist/
*.whl
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- Installer build artifacts (NSIS) ----
installer/build/
installer/dist/
installer/logs/
installer/publish/
installer/.tooling/

# ---- Release / binary artifacts (never in source) ----
*.exe
*.msi
*.sha256
*SHA256.txt
CHECKSUMS.sha256
MANIFEST.sha256

# Golden-project manifests ARE evidence inputs and must ship with the repo
# (they back the read-only golden checks). Re-include them explicitly.
!**/PROJECT_MANIFEST.sha256
!**/FACTORY_OVERLAY_MANIFEST.sha256

# ---- Agent / tooling working dirs (excluded by policy) ----
.workbuddy/
.aionrs/
.opencode/
night_run/
history/
artifacts/
evidence_summary/

# ---- Installer bundle staging (derived, rebuildable) ----
core/.tools/
installer/publish/
installer/dist/
installer/logs/
wheel/

# ---- OS / editor ----
.DS_Store
Thumbs.db
*.swp

# ---- Secrets / credentials (never commit) ----
.env
.env.*
*.env
