# Project Factory · P∞ UX5 Win11 Fluent Recovery Root

Authoritative current state: **Factory 0.14.2 worktree (MCP production line in progress) / UX5.1 shell accepted-rough / .NET 10 WPF + WPF-UI 4.3.0 / NSIS 3.12**.

Start with `RESTORE_FIRST.md`, then `CHECKPOINT_PINF_UX5_FLUENT_WPF_SOURCE_COMPLETE.md`.

The formal UI path no longer uses Tk/tkinter/ttkbootstrap. UX5 uses a self-contained Windows WPF shell with FluentWindow, Mica, NavigationView and Segoe UI Variable, while the exact 0.14.1 Python Core remains authoritative behind a JSON bridge.

Prior P12, P∞ UX1, UX3, UX3.1, UX3.2 and UX4 checkpoints/evidence remain included. Do not overwrite or delete them during recovery.

Current active gate: target-Windows WPF compile, real Windows 11 visual/interaction acceptance, and the NSIS install/launch/uninstall lifecycle. Static WPF checks are evidence of source-contract integrity, not proof of real rendering quality.
