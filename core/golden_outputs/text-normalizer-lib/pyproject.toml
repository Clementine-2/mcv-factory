[project]
name = "text-normalizer-lib"
version = "0.1.0"
description = "做一个 Python library，提供可复用的文本标准化能力，长期维护。"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
