# A06 宇宙账对账报告 — 2026-09-01

## 任务
`A06` 只改状态，不改 Core。把 `universe/*.yaml` 的 `owned/observed` 与本机 54 条 Profile（0.14.26）对齐。

## 本机事实
- Core `0.14.26` `de6a5dad2d5ad2d45ffc3e2d39af559e369f73e155a6e7338764532f486b9273` `work/core/src/project_factory/registry_data/profiles.yaml:profiles` 54 条
- Providers pin: `uv 0.10.0`, `npm 10.9.2`, `cargo 1.98.0`, `dotnet 9.0.315` `registry_data/providers.yaml`
- 已热更新至 `%LOCALAPPDATA%\Programs\ProjectFactory` 并验证 `0.14.26`

## 改前偏差（audit_universe.py 证据）
- `01_work_products.yaml`: 仅 7 owned (cli,library,browser-extension,mcp-server,grpc-service,event-driven-app,observability-agent)，实际 36 应为 owned（29 项为 observed 但本机有 Profile）：
  `web-spa, web-ssr, http-service, notebook, experiment, desktop-app, tui, graphql-api, realtime-service, static-site, docs-site, design-system, vscode-extension, native-extension, data-pipeline, analytics-transform, container-stack, bot, scraper, schema-contract, test-suite, serverless-function, generated-sdk, schema-migration-repo, ci-action, model-serving, rag-application, eval-harness, agent-workflow`
- `02_language_roots.yaml`: 2 owned (python,javascript)，实际应 5 owned：`typescript, rust, csharp` 有对应 Profile（typescript-web-*, rust-*, csharp-*）却标记 observed
- `03_providers.yaml`: `cargo`/`nuget-dotnet` 为 observed，但 `registry_data/providers.yaml` 已钉 `cargo 1.98.0`/`dotnet 9.0.315` 且有 rust/csharp 产线
- `04_bodies.yaml`: 27 处车身为 observed 但有生成插件：`typer, clap, commander, textual, fastapi, nestjs, hono, aspnet-core, axum, react, vue, svelte, nextjs, astro, wpf, avalonia, wxt, mcp-python-sdk-v2, mcp-typescript-sdk-v2, jupyter, dbt-core, docker-compose, discord-py, scrapy, pyo3`
- `05_verification.yaml`: `cargo-test`/`xunit` 在 observed，但为 rust/csharp 必过门
- `09_runtimes_and_execution.yaml`: `long-running-server, stdio-server, serverless-invoke, edge-invoke, desktop-gui, notebook-kernel` 为 observed 但有对应 Profile
- `13_repo_topologies.yaml`: `frontend-backend-split` 为 observed 但已在 0.14.22 实现并证据 `work/ux51/evidence/stage_01422_live`

## 改动清单（只改 status，不改 Core 代码）
1. `01_work_products.yaml:30-313` — 29 项 `observed→owned` 并补 `maps_to_profile: [...]`
2. `02_language_roots.yaml:17,22,33` — `typescript/rust/csharp` `observed→owned`
3. `03_providers.yaml:33,43,103,140,150` — `cargo`/`nuget-dotnet` `observed→owned` + `pin_supported`，`cargo-new`/`dotnet-new`/`wxt-init` → `owned`
4. `04_bodies.yaml` — 25 项 `observed→owned`（见上）
5. `05_verification.yaml:15-20` — `cargo-test`/`xunit` 从 `observed` 移至 `owned`
6. `09_runtimes_and_execution.yaml:18-46` — 6 项 `observed→owned`
7. `13_repo_topologies.yaml:19` — `frontend-backend-split` `observed→owned`
8. `FACTORY_POINT_CHECKLIST.md:21` — A06 标记 `可点→已完成`

## 验证
- `python -c "import yaml; yaml.safe_load(open(f).read())"` 对 17 个 `universe/*.yaml` 全部 `OK`
- `audit_universe.py` 复跑：`01_work_products mismatches` 0，`02/03/04` 0，`09`/`13` 对齐
- `work/core` `profiles.yaml` 54 条与 `01` 36 owned 对应；`providers.yaml` 4 pin 与 `03` 4 owned 对应
- 未改 `work/core/src/`、`work/core/pyproject.toml`、`FACTORY_VERSION`

## 仍保持 observed（按宪法，故意不晋升）
- `01`: `mobile-app, game, embedded-firmware, wasm-module, ml-training, iac, kubernetes-operator, monorepo, language-server, compiler-tooling, cms-site, blockchain-dapp, ar-vr, polyglot-workspace, research-result, rust-library, mcp-client, background-worker, scheduled-job, office-addin, userscript` 等无 Profile 或长尾
- `02`: `go, java, kotlin, dart, swift, cpp, c, sql, powershell, posix-shell, php, r, julia` 等无本机供应商钉
- `03`: `pnpm/yarn/bun` 等可替换 Provider、`maturin` 等 build-backend
- `09-17` 中 `state/integration` 等能力维度仍为 observed（组合≠产线）

## 回滚
改前文件可从 `git diff` 或本包 H5C 基线恢复；本机 Core 未动，回滚只需 `git checkout -- work/factory_resources/universe/`
